<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Primary color of the app (shown in the app bar) -->
    <color name="primary_color">#ff5722</color>

    <!-- Primary dark color of the app (shown in the status bar) -->
    <color name="primary_dark_color">#D32F2F</color>

    <!-- Background color for app -->
    <color name="tan_background">#FFF7DA</color>

    <!-- Background color for the song field -->
    <color name="field_songs">@android:color/white</color>

    <!-- Background color for the artists field -->
    <color name="field_artists">@android:color/white</color>

    <!-- Background color for albums field -->
    <color name="field_albums">@android:color/white</color>

    <!-- Background color for the store field -->
    <color name="field_store">@android:color/white</color>

    <!-- Background color for the search field -->
    <color name="field_search">#bdbdbd</color>
</resources>
